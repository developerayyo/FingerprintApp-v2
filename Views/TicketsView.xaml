<UserControl x:Class="ERPNextFingerprintApp.Views.TicketsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Main Ticket Panel -->
        <Border Grid.Column="0" Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Text="Employee Ticket" 
                         Style="{StaticResource HeaderTextStyle}"/>

                <!-- Fetch Ticket Button -->
                <Button Content="🎟 Fetch Ticket"
                      Style="{StaticResource PrimaryButtonStyle}"
                      Command="{Binding FetchTicketsCommand}"
                      IsEnabled="{Binding IsFetchEnabled}"
                      FontSize="16"
                      Padding="24,16"
                      Margin="0,0,0,16"/>

                <!-- Ticket Summary -->
                <Border Background="{StaticResource BackgroundBrush}" 
                      BorderBrush="{StaticResource BorderBrush}" 
                      BorderThickness="1" 
                      CornerRadius="4" 
                      Padding="12"
                      Margin="0,0,0,16"
                      Visibility="{Binding HasTickets, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <TextBlock Text="Available Ticket: " 
                                     Style="{StaticResource BodyTextStyle}" 
                                     FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding TicketCount}" 
                                     Style="{StaticResource BodyTextStyle}" 
                                     FontWeight="Bold"
                                     Foreground="{StaticResource PrimaryBrush}"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="16,0">
                            <TextBlock Text="Total: " 
                                     Style="{StaticResource BodyTextStyle}" 
                                     FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding TotalAmount}" 
                                     Style="{StaticResource BodyTextStyle}" 
                                     FontWeight="Bold"
                                     Foreground="{StaticResource SuccessBrush}"/>
                        </StackPanel>
                        
                        <Button Grid.Column="2"
                              Content="🎯 Use All Ticket"
                              Style="{StaticResource SecondaryButtonStyle}"
                              Command="{Binding UseAllTicketsCommand}"
                              IsEnabled="{Binding HasMultipleTickets}"
                              Visibility="{Binding HasMultipleTickets, Converter={StaticResource BooleanToVisibilityConverter}}"
                              Padding="12,6"
                              FontSize="12"/>
                    </Grid>
                </Border>

                <!-- Ticket DataGrid -->
                <Border BorderBrush="{StaticResource BorderBrush}" 
                      BorderThickness="1" 
                      CornerRadius="4"
                      Visibility="{Binding HasTickets, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <DataGrid x:Name="TicketsDataGrid"
                            ItemsSource="{Binding Tickets}"
                            AutoGenerateColumns="False"
                            CanUserAddRows="False"
                            CanUserDeleteRows="False"
                            CanUserReorderColumns="False"
                            CanUserResizeRows="False"
                            CanUserSortColumns="True"
                            IsReadOnly="True"
                            SelectionMode="Single"
                            GridLinesVisibility="Horizontal"
                            HeadersVisibility="Column"
                            Background="{StaticResource SurfaceBrush}"
                            RowBackground="{StaticResource SurfaceBrush}"
                            AlternatingRowBackground="{StaticResource BackgroundBrush}"
                            BorderThickness="0"
                            FontSize="13">
                        
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="12,8"/>
                                <Setter Property="BorderThickness" Value="0,0,1,0"/>
                                <Setter Property="BorderBrush" Value="#4CAF50"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Height" Value="40"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="12,8"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Ticket Type" 
                                              Binding="{Binding TicketType}" 
                                              Width="*"
                                              MinWidth="120"/>
                            
                            <DataGridTextColumn Header="Amount" 
                                              Binding="{Binding FormattedAmount}" 
                                              Width="100"
                                              ElementStyle="{StaticResource RightAlignedTextStyle}"/>
                            
                            <DataGridTextColumn Header="Status" 
                                              Binding="{Binding Status}" 
                                              Width="80"/>
                            
                            <DataGridTemplateColumn Header="Action" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="🎫 Use"
                                              Style="{StaticResource PrimaryButtonStyle}"
                                              Command="{Binding DataContext.UseTicketCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              CommandParameter="{Binding}"
                                              IsEnabled="{Binding IsUnused}"
                                              Padding="8,4"
                                              FontSize="11"
                                              Margin="4,2"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <!-- No Ticket Message -->
                <Border Background="{StaticResource BackgroundBrush}" 
                      BorderBrush="{StaticResource BorderBrush}" 
                      BorderThickness="1" 
                      CornerRadius="4" 
                      Padding="40"
                      Margin="0,16,0,0"
                      Visibility="{Binding HasTickets, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Invert}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="🎟" 
                                 FontSize="48" 
                                 HorizontalAlignment="Center" 
                                 Margin="0,0,0,16"
                                 Opacity="0.5"/>
                        <TextBlock Text="No Available Ticket" 
                                 Style="{StaticResource SubHeaderTextStyle}" 
                                 HorizontalAlignment="Center"
                                 Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock Text="Click 'Fetch Ticket' to verify your fingerprint and load available Ticket" 
                                 Style="{StaticResource BodyTextStyle}" 
                                 HorizontalAlignment="Center"
                                 TextAlignment="Center"
                                 Foreground="{StaticResource TextSecondaryBrush}"
                                 Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Action Buttons -->
                <StackPanel Orientation="Horizontal" 
                          HorizontalAlignment="Right" 
                          Margin="0,16,0,0"
                          Visibility="{Binding HasTickets, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Button Content="🔄 Refresh"
                          Style="{StaticResource SecondaryButtonStyle}"
                          Command="{Binding RefreshTicketsCommand}"
                          Padding="12,8"
                          Margin="0,0,8,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Status & Information Panel -->
        <Border Grid.Column="2" Style="{StaticResource CardStyle}">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Status" 
                             Style="{StaticResource SubHeaderTextStyle}"/>
                    <Button Grid.Column="1" Content="🔄" 
                          Style="{StaticResource SecondaryButtonStyle}"
                          Width="30" Height="30" 
                          FontSize="12"
                          ToolTip="Refresh Status"
                          Command="{Binding RefreshTicketsCommand}"
                          Margin="8,0,0,0"/>
                </Grid>
                
                <TextBlock Text="{Binding StatusMessage}"
                         Style="{StaticResource BodyTextStyle}"
                         TextWrapping="Wrap"
                         Margin="0,8,0,16"/>

                <ProgressBar IsIndeterminate="{Binding IsLoading}"
                           Height="4"
                           Background="{StaticResource BorderBrush}"
                           Foreground="{StaticResource PrimaryBrush}"
                           Margin="0,0,0,16"/>

                <!-- SDK Status Section -->
                <TextBlock Text="SDK Status:" 
                         Style="{StaticResource SubHeaderTextStyle}"
                         Margin="0,0,0,4"/>
                <Border Background="#F8F9FA" 
                      BorderBrush="{StaticResource BorderBrush}" 
                      BorderThickness="1" 
                      CornerRadius="4" 
                      Padding="8"
                      Margin="0,0,0,12">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <TextBlock Text="Installation:" 
                                     Style="{StaticResource CaptionTextStyle}" 
                                     Width="80"/>
                            <TextBlock x:Name="TicketsSdkStatusText" 
                                     Text="Not Installed" 
                                     Foreground="#FF6B6B" 
                                     FontSize="11" 
                                     FontWeight="SemiBold"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Device:" 
                                     Style="{StaticResource CaptionTextStyle}" 
                                     Width="80"/>
                            <TextBlock x:Name="TicketsDeviceStatusText" 
                                     Text="Not Connected" 
                                     Foreground="#FF6B6B" 
                                     FontSize="11" 
                                     FontWeight="SemiBold"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <TextBlock Text="Instructions:" 
                         Style="{StaticResource SubHeaderTextStyle}"/>
                <TextBlock Style="{StaticResource CaptionTextStyle}" TextWrapping="Wrap">
                    1. Click "Fetch Ticket" to verify fingerprint<LineBreak/>
                    2. View available Ticket in the grid<LineBreak/>
                    3. Use individual Ticket or all at once<LineBreak/>
                    4. Each action requires fingerprint verification<LineBreak/>
                    5. Ticket are automatically updated in ERPNext
                </TextBlock>

                <!-- Quick Stats -->
                <Border Background="{StaticResource PrimaryBrush}" 
                      CornerRadius="4" 
                      Padding="12"
                      Margin="0,16,0,0"
                      Visibility="{Binding HasTickets, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="📊 Quick Stats" 
                                 Foreground="White" 
                                 FontWeight="SemiBold"
                                 Margin="0,0,0,8"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Ticket:" 
                                         Foreground="#E8F5E8" 
                                         FontSize="10"/>
                                <TextBlock Text="{Binding TicketCount}" 
                                         Foreground="White" 
                                         FontSize="16"
                                         FontWeight="Bold"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Total Value:" 
                                         Foreground="#E8F5E8" 
                                         FontSize="10"/>
                                <TextBlock Text="{Binding TotalAmount}" 
                                         Foreground="White" 
                                         FontSize="16"
                                         FontWeight="Bold"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>